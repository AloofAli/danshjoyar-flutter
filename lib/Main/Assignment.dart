 class Assignment {
    String? _subject;// The subject of the assignment
     // final DateFormat dateFormat = new SimpleDateFormat("yyyy/MM/dd ");// Date format for deadlines
     // Date deadLine; // The deadline for completing the assignment
     bool _Active = false;// Indicates whether the assignment is currently active    /**ructs
        }